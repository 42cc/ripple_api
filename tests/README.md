`test_proj` contains dummy Django project. You can use it to run tests:

    $ cd test_proj
    $ virtualenv .env
    $ source .env/bin/activate
    $ pip install -r requirements.txt
    $ python manage.py test ripple_api
